<IfModule mod_rewrite.c>
    RewriteEngine On

    # Если запрос не к папке /public, перенаправляем в /public
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]

</IfModule>